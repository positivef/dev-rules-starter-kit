# Development dependencies for dev-rules-starter-kit
# Install with: pip install -r requirements-dev.txt

# Core dependencies (from requirements.txt)
-r requirements.txt

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0

# Development tools
flask>=2.3.0  # For backend_api tests
watchdog>=3.0.0  # For dev_assistant tests

# Optional test dependencies
# Note: precision_wrapper is a custom module, not available on PyPI
# If you need to test precision_integration.py, you need to install it separately

# Code quality tools (already in requirements.txt but listed for clarity)
# pre-commit==3.7.1
# ruff==0.4.4

# Documentation tools (optional)
mkdocs>=1.5.0
mkdocs-material>=9.0.0

# Type checking (optional)
mypy>=1.0.0
types-PyYAML>=6.0.0
